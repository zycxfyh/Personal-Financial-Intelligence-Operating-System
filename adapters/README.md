# Adapters

`adapters/` holds replaceable integrations that implement system contracts without defining system meaning.

Current status:

- introduced with Hermes runtime first
- old entrypoints may remain as compatibility shims
