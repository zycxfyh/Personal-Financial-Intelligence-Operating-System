# Scheduler

Infrastructure-owned trigger registration and dispatch.

- It decides `when` to invoke capability/orchestrator entrypoints.
- It does not decide business semantics.
- It does not mutate state truth directly.
