[allowlist]
  description = "Global allowlist"
  paths = [
    '''pnpm-lock\.yaml''',
    '''\.git/''',
    '''\.venv/''',
  ]
  regexes = [
    # Allow docs mentioning secrets conceptually (not actual keys)
    '''docs/.*\.md''',
  ]
